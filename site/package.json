{
  "private": true,
  "name": "site",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "gatsby clean && gatsby build && gatsby serve",
    "develop": "gatsby clean && gatsby develop",
    "clean": "gatsby clean"
  },
  "dependencies": {
    "gatsby": "^4.24.6",
    "gatsby-plugin-i18n-config": "*",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
